[project]
name = "tia_composer"
version = "0.1.3"
description = "TIA Composer"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "pydantic>=2.0.0",
    "pydantic-settings>=2.0.0",
    "pythonnet>=3.0.5",
    "toml>=0.10.0",
]

[project.scripts]
tia-composer = "tia_composer:main"
